# Dependencies
node_modules/

# Config with secrets
/config.json
.env

# OS
.DS_Store

# IDE
.vscode/
.idea/
coverage/

# Internal planning / handoff / notes — contain internal ticket IDs (API-xxx).
# nansen-cli is a public repo; these must never be committed. Kept in the working
# tree so local agents (Claude, Codex, …) can read them.
/.notes/
/*_PLAN.md
